-- Once you're done with this, rename the file to "env.luau"
-- env.luau is already added to the .gitignore for security purposes.

self = {}

self.ApiKey = "sk-TO-BE-FILLED-OUT-BY-END-USER" -- Replace the key with your actual key of course.

return self
